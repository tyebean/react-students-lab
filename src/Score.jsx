
// Code the <Score> component as a function component that renders the score object's date & score properties.

const Score = () => {
  return ( 
    <div>
      this is Score
    </div>
  );
}

export default Score;